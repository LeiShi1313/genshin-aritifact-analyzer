#Deepwood Alhaitham w/ flexible combo to adapt to energy (essentially, burst every other rot if low ER). Customized random frame delays; 12-3 Icewind Suite energy.

alhaitham char lvl=90/90 cons=0 talent=9,9,9;
alhaitham add weapon="harbingerofdawn" refine=5 lvl=90/90;
alhaitham add set="deepwood" count=4;
#alhaitham add set="gilded" count=4;
alhaitham add stats hp=4780 atk=311 em=186.5 dendro%=0.466 cr=0.311; #69.15 [97.15] / 176.12 | 111.01 er
alhaitham add stats hp=507.88 atk=33.07 def=39.35 hp%=0.0991 atk%=0.0991 def%=0.1239 em=79.26 er=0.1101 cr=0.3305 cd=0.7926;
                   #hp 2[0]   atk 2[0]  def 2[0]  hp% 2[0]   atk% 2[0]   def% 2[0]   em 4[2]  er 2[0]   cr 10[8]  cd 12[10]

xingqiu char lvl=90/90 cons=6 talent=9,9,9;
xingqiu add weapon="favsword" refine=3 lvl=90/90;
xingqiu add set="eosf" count=4;
#xingqiu add set="deepwood" count=4;
#xingqiu add set="noblesse" count=4;
xingqiu add stats hp=4780 atk=311 atk%=0.466 hydro%=0.466 cr=0.311; #69.15 / 129.26 | 171.26 er
xingqiu add stats hp=507.88 atk=33.07 def=39.35 hp%=0.0991 atk%=0.1982 def%=0.1239 em=39.63 er=0.1101 cr=0.3305 cd=0.7926;
                 #hp 2[0]   atk 2[0]  def 2[0]  hp% 2[0]   atk% 4[2]   def% 2[0]   em 2[0]  er 2[0]   cr 10[8]  cd 12[10]

kuki char lvl=90/90 cons=6 talent=9,9,9;
kuki add weapon="ironsting" refine=5 lvl=90/90;
kuki add set="fopl" count=4;
kuki add stats hp=4780 atk=311 em=186.5 em=186.5 em=186.5; #44.66 / 102.84 | 111.01 er
kuki add stats hp=507.88 atk=33.07 def=39.35 hp%=0.0991 atk%=0.0991 def%=0.1239 em=118.89 er=0.1101 cr=0.3966 cd=0.5284;
              #hp 2[0]   atk 2[0]  def 2[0]  hp% 2[0]   atk% 2[0]   def% 2[0]   em 6[4]   er 2[0]   cr 12[10] cd 8[6]

diona char lvl=90/90 cons=6 talent=9,9,9;
diona add weapon="favbow" refine=4 lvl=90/90;

diona add set="instructor" count=4;
diona add stats hp=3571 atk=232 er=0.518 hp%=0.348 cr=0.232; #62.58 / 60.57 | 229.66 er
diona add stats hp=406.3 atk=26.45 def=31.485 hp%=0.07925 atk%=0.07925 def%=0.0991 em=31.71 er=0.1761 cr=0.2116 cd=0.1057;
#4-star subs    hp 2[0]  atk 2[0]  def 2[0]   hp% 2[0]    atk% 2[0]    def% 2[0]   em 2[0]  er 4[2]    cr 8[6]   cd 2[0]
diona add stats cr=0.1322;
#5-star subs    cr 4[4]

#diona add set="deepwood" count=4;
#diona add set="noblesse" count=4;
#diona add stats hp=4780 atk=311 er=0.518 hp%=0.466 cr=0.311; #69.15 / 89.63 | 223.06 er
#diona add stats hp=507.88 atk=33.07 def=39.35 hp%=0.4955 atk%=0.0991 def%=0.1239 em=39.63 er=0.1101 cr=0.3305 cd=0.3963;
               #hp 2[0]   atk 2[0]  def 2[0]  hp% 10[8]  atk% 2[0]   def% 2[0]   em 2[0]  er 2[0]   cr 10[8]  cd 6[4]

fn randy(mean, standardDeviation, min, max, display) {
  let value = randnorm() * standardDeviation + mean;
  if value > max {
    value = max;
  } else if value < min {
    value = min;
  }
  if display {
    print(value);
  }
  return value;
}


let prev_char_id = -1;
let prev_action_id = -1;


let _execute_action = execute_action;
fn execute_action(char_id number, action_id number, p map) {
  #print(prev_char_id, " ", prev_action_id, " ", char_id, " ", action_id);


  # this next if block handles implict swap or otherwise any pre swap delay specified in this function
  # will not work
  if prev_char_id != char_id && action_id != .action.swap {
    execute_action(char_id, .action.swap, []);
  }


  # this following if block handles conditionally adding in delay depending on
  # what the previous and next action is. in this example, delay is not added between attacks
  # and charge attack following an attack
  # modify these conditions as you see fit
  if action_id == .action.swap {
    # add delay before swap
    delay(randy(9.5, 1.5, 5, 17, 0));
  } else if prev_action_id == .action.attack && action_id != .action.attack && action_id != .action.charge {
    # add delay after attack, but only if not followed by another attack or charge
    delay(randy(3, 1, 0, 6, 0));
  } else if prev_action_id != .action.attack {
    # add delay to everything else
    delay(randy(3, 1, 0, 6, 0));
  }


  # this here tracks the previous character id so that it can be used above
  prev_char_id = char_id;
  prev_action_id = action_id;
  return _execute_action(char_id, action_id, p);
}

# --- Options
options swap_delay=1 iteration=1000 ignore_burst_energy=false;

# --- Targets
target lvl=100 resist=0.1 hp=999999999999 radius=2 pos=0,2.5 particle_threshold=520000 particle_drop_count=3; #Energy drops based on 12-3 Icewind Suite

active xingqiu;

#Xingqiu E Q N1 > Diona E N1 Q > Kuki N1 E Dash > [Alhaitham APL stuff]
for let r=0; r<4; r=r+1 {
  xingqiu skill;
  while !.xingqiu.burst.ready {
    if .xingqiu.skill.ready {
      xingqiu skill;
    } else {
      xingqiu attack;
    }
  }
  xingqiu burst, attack;

  diona skill[hold=1], attack;
  while !.diona.burst.ready {
    if .diona.skill.ready {
      diona skill[hold=1];
    } else {
      diona attack;
    }
  }
  diona burst;

  kuki attack, skill, dash;

  alhaitham attack;
  let alhaithamStart = f();
  while !.xingqiu.skill.ready {
    switch {
      case .alhaitham.burst.ready && ((f() == alhaithamStart) || ((.xingqiu.skill.cd > 14*60) && (.alhaitham.mirrors < 2))):
        alhaitham burst;
        if .kuki.burst.ready {
          kuki burst;
        } else {
          xingqiu attack:2;
        }
        alhaitham swap;
        let waitStart = f();
        while !.alhaitham.mirrors && (f() - waitStart < 120) {
          alhaitham attack;
        }

      case .alhaitham.skill.ready && (.alhaitham.mirrors == 0):
        alhaitham skill[hold=1], low_plunge[short=1];

      case !.status.alhaitham-a1-icd && (.alhaitham.mirrors < 3):
        if .alhaitham.normal == 1 {
          alhaitham attack;
        }
        alhaitham charge, dash;

      case .alhaitham.normal > 3:
        alhaitham dash;

      default:
        alhaitham attack;
    }
  }
  #rotation over
}
