fn rand_delay(mean, stddev) {
  let del = randnorm() * stddev + mean;
  if del > (mean + mean) {
    del = mean + mean;
  }
  delay(del);
}

let prev_char_id = -1;
let prev_action_id = -1;

let _execute_action = execute_action;
fn execute_action(char_id number, action_id number, p map) {
  #print(prev_char_id, " ", prev_action_id, " ", char_id, " ", action_id);

  # this next if block handles implict swap or otherwise any pre swap delay specified in this function
  # will not work
  if prev_char_id != char_id && action_id != .action.swap {
    execute_action(char_id, .action.swap, []);
  }

  # this following if block handles conditionally adding in delay depending on
  # what the previous and next action is. in this example, delay is not added between attacks
  # and charge attack following an attack
  # modify these conditions as you see fit
  if action_id == .action.swap {
    # add delay before swap
    rand_delay(12, 3);
  }

  # this here tracks the previous character id so that it can be used above
  prev_char_id = char_id;
  prev_action_id = action_id;
  return _execute_action(char_id, action_id, p);
}

# --- Team
navia char lvl=90/90 cons=0 talent=9,9,9;
navia add weapon="serpentspine" refine=1 lvl=90/90 +params=[stacks=5];
navia add set="nighttimewhispers" count=5;
navia add stats hp=4780 atk=311 atk%=0.466 geo%=0.466 cr=0.311 ; #main
navia add stats def=39.36 def%=0.124 hp=507.88 hp%=0.0992 atk=33.08 atk%=0.1984 er=0.1102 em=39.64 cr=0.331 cd=0.7944 ;

furina char lvl=90/90 cons=0 talent=9,9,9;
furina add weapon="favoniussword" refine=3 lvl=90/90;
furina add set="totm" count=4;
furina add stats hp=4780 atk=311 hp%=0.466 hydro%=0.466 cr=0.311 ; #main
furina add stats def=39.36 def%=0.124 hp=507.88 hp%=0.1984 atk=33.08 atk%=0.0992 er=0.1102 em=39.64 cr=0.331 cd=0.7944 ;

#Normal 4GT Furina Build
#furina add set="gt" count=4;
#furina add stats hp=4780 atk=311 hp%=0.466 hp%=0.466 cr=0.311 ; #main
#furina add stats def=39.36 def%=0.124 hp=507.88 hp%=0.1984 atk=33.08 atk%=0.0992 er=0.1102 em=39.64 cr=0.331 cd=0.7944 ;

fischl char lvl=90/90 cons=6 talent=9,9,9;
fischl add weapon="stringless" refine=3 lvl=90/90;
fischl add set="gt" count=4;
fischl add stats hp=4780 atk=311 em=187 electro%=0.466 cr=0.311;
fischl add stats def%=0.124 def=39.36 hp=507.88 hp%=0.0992 atk=33.08 atk%=0.0992 er=0.3306 em=39.64 cr=0.2648 cd=0.7944;

baizhu char lvl=90/90 cons=0 talent=9,9,9;
baizhu add weapon="thrillingtalesofdragonslayers" refine=5 lvl=90/90;
baizhu add set="ohc" count=4;
#baizhu add set="no" count=4;
baizhu add stats hp=4780 atk=311 er=0.518 hp%=0.466 heal=0.359 ; #main
baizhu add stats def=39.36 def%=0.124 hp=507.88 hp%=0.496 atk=33.08 atk%=0.0992 er=0.551 em=39.64 cr=0.1986 cd=0.1324;

# --- Options
options iteration=1000;
energy every interval=480,720 amount=1;

# --- Targets
target lvl=100 resist=0.1 radius=2 pos=0,2.4 hp=999999999;

# --- Rotation
active furina;

for let i=0; i<4; i=i+1 {
    furina skill, dash, attack;                    pick_up_crystallize("any");
    fischl attack:2, skill, dash;                  pick_up_crystallize("any");
    navia burst;
    furina burst;
    baizhu skill, burst;
      print(.navia.shrapnel);
    navia skill[hold=1], attack:3, dash;           pick_up_crystallize("any");
    navia attack:2;
    fischl attack:2, burst;                        pick_up_crystallize("any"); #With fischl burst
      print(.navia.shrapnel);
    navia skill[hold=1], attack:3, dash;           pick_up_crystallize("any");
    navia attack:2;
    baizhu skill, dash, attack;                    pick_up_crystallize("any");
      print(.navia.shrapnel);
    if !.navia.skill.ready {
    navia attack, dash;                            pick_up_crystallize("any");
    }
    navia skill[hold=1], attack:3, dash;           pick_up_crystallize("any");
    navia attack:2;
}
#Furina E dash N1 > Fischl E dash >
#Navia Q > Furina Q > Baizhu EQ >
#Navia hE N3D N2 > Fischl Q >
#Navia hE N3D N2 > Baizhu E dash >
#Navia hE N3D N2 (if this 3rd E is on cooldown, add 'N1 dash' before E)
